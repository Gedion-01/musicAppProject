import { createRef } from 'react';

export const animationRef: any = createRef();
export const audioRef: any = createRef();
export const progressBarRef: any = createRef()
export const currentTimeRef: any = createRef()